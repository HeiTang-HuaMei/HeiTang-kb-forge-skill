# Android Scaffold

This directory reserves the Android target for the Flutter Workbench.

When Flutter is installed, run from `web/workbench/flutter_app`:

```powershell
flutter create --platforms=android .
flutter run -d android
```

No Core runtime code is imported by this scaffold.
