# Web And PWA Scaffold

This directory reserves the Flutter Web and PWA target for the Workbench.

The static Web prototype also keeps `web/workbench/manifest.webmanifest`; Flutter Web keeps `web/manifest.json`.
