# iOS Scaffold

This directory reserves the iOS target for the Flutter Workbench scaffold.

When the Flutter CLI is available, run:

```bash
flutter create --platforms=ios .
flutter run -d ios
```

The current repository scaffold includes the iOS target metadata and app delegate placeholder without adding Core runtime dependencies.
