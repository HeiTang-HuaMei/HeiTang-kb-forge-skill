# Regression Check

v2.5 回归检查覆盖 v1.6 到 v2.4，检查本地命令、模块、schema 和测试证据。

输出 `regression_result.json`、`regression_report.md`、`regression_cases.jsonl` 和 `regression_failures.jsonl`。

它不运行外部 runtime，也不调用在线服务。

