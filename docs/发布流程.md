# 发布流程

## 当前禁止项

- 不进入 Campaign 4。
- 不创建 GitHub Release。
- 不创建稳定 `campaign-1-3-baseline` tag。

## 当前允许项

在 v4.2 Clean Public Repository Reset 完成并推送后，只允许创建：

```text
campaign-1-3-baseline-rc.4
```

然后验证 CI 与 Release Check。验证完成后停止。

Quickstart 必须先能完成本地安装、doctor、build、check-contract、kb-query 和 generate-documents。Release Check 必须覆盖 `python -m pytest`、Quickstart build、Doctor、Release readiness 与 `git diff --check`。

## tag 口径

- `campaign-1-3-baseline-rc.N`：战役基线候选验证 tag。
- `campaign-1-3-baseline`：稳定战役基线 tag，当前禁止创建。
- `v4.2.x` / `v4.3.x`：真实产品版本 tag，当前任务不创建。
