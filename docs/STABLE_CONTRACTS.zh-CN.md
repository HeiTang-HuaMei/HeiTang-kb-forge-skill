# 稳定版契约检查

v2.0 新增 `stable-check`，用于检查 workspace 是否包含知识包、Skill 包、Agent 包、workspace、provider registry、prompt profile registry 和 LLM audit 等稳定版文件。

`extension_readiness` 是后续版本扩展预留字段。v2.0 中母版 Skill 学习、派生 Skill 生成、平台分发、XHS 导出等后续能力均明确标记为 `not_enabled`。
