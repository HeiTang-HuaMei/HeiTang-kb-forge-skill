# Parser Hardening

Parser hardening records parser status, source type, file hash, file size, parser version, parse status, warnings, and chunk count.

It is a reporting layer. It does not claim perfect layout reconstruction, OCR correction, table reasoning, or semantic understanding.
