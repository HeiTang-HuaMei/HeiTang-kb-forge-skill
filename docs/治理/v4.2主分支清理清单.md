# v4.2 主分支清理清单

生成时间：2026-06-14T15:17:56+08:00

## 清理目标

让 public main 分支只代表当前 v4.2 产品基线。旧 RC、旧 Campaign 中间证据、pre-v4.2 审计堆和临时 current_run/latest 输出从 main 移除，由 Git history 保留。

## 清理前盘点

- `git status --short` 已执行。
- `git ls-tree --name-only HEAD` 已执行。
- `git ls-tree -r --name-only HEAD` 已执行，递归清单保存到本地 ignored current_run 日志。
- 清理前 HEAD 跟踪文件数量：3751。
- 清理前根目录旧 JSON 残留：`final_fix_log.json`、`final_v4_rc_gate_report.json`、`v4_rc_final_gate_report.json`、`v310_external_absorption_map.json`、`v312_external_absorption_map.json`、`v38_external_absorption_map.json`、`v39_external_absorption_map.json`。

## 执行动作

- 删除 main 中旧根目录 JSON 报告。
- 删除 main 中 `docs/audits/**` 历史审计堆。
- 删除 main 中 `artifacts/**` 历史审计堆和旧 Campaign 中间证据。
- 删除 main 中旧英文 docs 堆，替换为中文公开文档。
- 删除旧 `.agents/**` 外部参考残留。
- 保留 Python package、tests、scripts、examples、assets、desktop、packaging、GitHub workflows。
- 更新 README 链接到中文 docs 公开面。
- 新增 v4.2 public repository reset 硬测试。

## 禁止项

- 不进入 Campaign 4。
- 不创建 GitHub Release。
- 不创建稳定 `campaign-1-3-baseline` tag。
- 不把旧文件搬进 `docs/archive` 或 `artifacts/archive` 制造新的垃圾堆。
- 不删除 active v4.2 runtime / test 文件。
