# 目标验收矩阵

## 当前基线

当前 public main 只表达 v4.2 产品基线。验收重点是本地 Core 能力、文档输出、Skill/Agent 包边界和仓库公开表面清洁度。

## 四类产品产物

| 产物 | 当前状态 | 边界 |
| --- | --- | --- |
| Knowledge Package | active_core_capability | 本地构建、可追溯、可检索 |
| Document Outputs | active_core_capability | Markdown / DOCX / PDF / PPTX |
| Skill Outputs | active_core_capability | Skill Template / Skill Suite |
| Agent Creation Package | package_capability | 不是 Agent Runtime ready |

状态词保留：`completed_baseline` 表示 v4.2 public main 的当前 Core 基线能力；`not_started` 表示后续战役未启动；`future target` 表示未来候选目标，不是当前 runtime。

## 禁止误判

- Campaign 4 未启动。
- Campaign 5 未启动。
- GitHub Release 未创建。
- 稳定 `campaign-1-3-baseline` tag 未创建。
- Redis / Vector DB runtime 未作为当前能力接入。
