# Local Workspace

v1.9 adds a portable local workspace for knowledge packages, Skill packages, Agent packages, registries, reports, exports, logs, and templates.

```powershell
python -m heitang_kb_forge.cli workspace-init --workspace .\workspace
```

The workspace stores paths by default and does not force-copy large packages.
