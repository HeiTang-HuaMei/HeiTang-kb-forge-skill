# Studio Industrial Console

v2.2 checkpoint fill adds read-only local Studio files:

- `action_center.json`
- `run_history.jsonl`
- `studio_v22_summary.json`

Studio remains a local presentation layer over file outputs. It is not SaaS, not a permission system, and not the core engine.
