# 可靠性评分

`reliability-score` 为本地 workspace 生成 `reliability_score.json` 和 `reliability_report.md`。

评分是离线规则评分，检查 workspace 完整度、知识包登记、Skill / Agent 可用性、provider registry 和 audit trace。它是发布就绪信号，不是在线评估系统。
