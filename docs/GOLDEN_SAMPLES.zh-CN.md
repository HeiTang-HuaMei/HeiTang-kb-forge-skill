# Golden Samples

v2.5 新增本地 golden sample 验证，覆盖知识包、Skill 包、Agent 包、workspace snapshot 和平台导出包。

输出 `golden_sample_registry.json`、`golden_sample_validation.json`、`golden_sample_validation_report.md` 和 `golden_sample_diff.json`。

Golden samples 是本地验证 fixture，不是真实平台集成。

