# 本地工作区

v1.9 新增 portable local workspace，用于管理知识包、Skill 包、Agent 包、registries、reports、exports、logs 和 templates。

```powershell
python -m heitang_kb_forge.cli workspace-init --workspace .\workspace
```

默认登记路径，不强制复制大文件。
