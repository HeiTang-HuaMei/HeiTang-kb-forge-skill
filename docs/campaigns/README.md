# Campaign 历史文档说明

历史文档，仅作追溯；当前产品基线以 `docs/product/` 下三份 v3 文档为准。本文目录下旧 Campaign 报告、旧 RC 报告、旧验收记录和旧能力矩阵不得作为当前功能、架构或验收事实源。

当前主链路统一为：

```text
文档库 → 知识库 → 索引层 → RAG → 编排层 → 文档/Skill/Agent/A2A
```

如旧文件出现 Campaign、rc、runtime ready、release ready 等表述，均按历史语境理解，不代表当前已接入能力。
