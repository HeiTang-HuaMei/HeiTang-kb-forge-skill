# v4.3.0-rc6 Yellow Status Final Resolution Matrix

Date: 2026-06-17

| Capability | rc6 UI Truth State | Resolution |
| --- | --- | --- |
| File import | `enabled_real` on Windows EXE with real file picker; web remains disabled boundary | Real EXE imported `rc6_truth_hard_source.md` and wrote `source_manifest.json` |
| Document Library | `waiting_for_input` until real import; examples are `display_only` or disabled boundary | No static parsed examples are accepted as real |
| Knowledge Base | `enabled_real` only after non-empty manifest/chunks/quality artifacts | Real EXE wrote `kb/chunks.jsonl`, `kb/manifest.json`, and `kb/quality_report.json` |
| Search | `enabled_real` only after real query result is produced | Real EXE wrote `query/kb_query_result.json`; `selected_count=5` |
| Document generation | `enabled_real` only after non-empty Markdown is produced | Real EXE wrote `doc/generated.md` containing the imported-file keyword |
| Skill draft | `enabled_real` only after Skill package path exists | Real EXE wrote `skill/SKILL.md`, `skill_manifest.yaml`, and `skill_manifest.json` |
| Agent draft | `enabled_real` only after Agent package path exists | Real EXE wrote `agent/agent_manifest.json` and bound Skill manifest |
| Settings save/config | Campaign 7 config remains accepted and UI-bound | Reused, not reimplemented |
| Workbench Bridge allowlisted action | `enabled_real` only through allowlisted Core bridge | No arbitrary shell |
| Computer Use | `disabled_boundary` | Not implemented |
| arbitrary shell | No executable entry | Blocked by bridge validation |
| unauthorized plugin marketplace | `disabled_boundary` / no executable entry | Not opened |
| external vector DB | `disabled_boundary` until configured | Not falsely marked connected |
| external network Provider | disabled until opt-in / configured | No unauthorized expansion |

Final EXE-visible yellow status review passed on the rc6 runtime panel. Safety boundaries remain orange/disabled only where the capability is intentionally not opened.
