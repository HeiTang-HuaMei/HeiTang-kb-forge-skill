# v4.3.0-rc5 Owner Retest Checklist

Date: 2026-06-17

Candidate: `v4.3.0-rc5`

Target status before Owner retest: `v4.3.0-rc5_full_capability_audit_repaired_pushed_ci_green_tagged_pending_owner_retest`

## Start

1. Launch the rebuilt `heitang_workbench.exe`.
2. Confirm the app opens without white screen, black screen, or crash.
3. Confirm no duplicate in-page minimize/maximize/close controls are visible.
4. Confirm native Windows minimize, maximize, restore, resize, and close still work.

## Layout

| Viewport | Expected |
| --- | --- |
| 1440x900 | No right-side clipping; main content and status/action panels reachable. |
| 1366x768 | Sidebar and lower navigation reachable; main page scroll works. |
| Windows scaling | Key buttons, status labels, tabs, and error messages remain visible. |

## Page Retest

| Page | Must verify |
| --- | --- |
| Dashboard | Accepted status surfaces visible; no stale rc4 current-candidate claim. |
| Import / Parsing | `Real Runtime Chain` visible; `Document Preflight` and `Batch Import Documents` show Core run buttons and feedback path. |
| Document Library / Knowledge Base | Document import/preflight status visible; knowledge build/package actions available where shown. |
| Retrieval / Verification | Query/evidence actions available; source/citation/degraded messaging visible. |
| Document Generation | Markdown/document bundle generation actions visible; output path or failure message shown after run. |
| Skill Factory | Skill package/governance actions visible; real package/report path or failure message shown after run. |
| Agent Factory | Standalone and KB-bound Agent generation actions visible; Computer Use remains disabled. |
| Workbench Bridge | Allowlisted action status visible; no arbitrary shell entry. |
| Settings | Provider/config readiness visible; secrets are masked; no raw API key shown. |
| Campaign 9 Desktop Delivery | Shows `v4.3.0-rc5` and pending Owner retest; no GitHub Release or stable tag claim. |

## Runtime Chain Spot Checks

Run at least one local action from each group where local workspace inputs are available:

- document preflight or batch import;
- knowledge package build;
- RAG query or evidence selection;
- document generation;
- Skill generation or governance report;
- standalone or KB-bound Agent generation;
- provider config validation or readiness.

Expected result for each run:

- visible running state;
- success, retryable failure, or blocked/degraded state;
- sanitized output;
- output path inside workspace when an artifact is produced;
- no raw secret in UI or logs.

## Boundary Checks

| Boundary | Expected |
| --- | --- |
| Computer Use | `disabled_boundary`; no OS/browser/screen/keyboard/mouse runtime enabled. |
| arbitrary shell | no entry and no executable shell path exposed. |
| unauthorized plugin marketplace | absent or disabled with reason. |
| third-party API execution | config-gated; no undeclared API call. |
| secrets | masked only; never raw in UI/report/log/fixture. |
| GitHub Release | not created. |
| stable `v4.3.0` | not tagged unless Owner separately authorizes. |

## Retest Exit Options

If Owner retest passes:

`v4.3.0-rc5_owner_retest_passed_pending_stable_release_decision`

If Owner retest fails:

`v4.3.0-rc5_owner_retest_failed_pending_rc6_or_scope_cut`
