# v4.3.0-rc7 Owner Retest Checklist

Target status after Owner retest:

`v4.3.0-rc7_capability_mapping_product_flow_repaired_real_exe_verified_tagged_pending_owner_retest`

Do not create a GitHub Release during this retest. Do not tag stable `v4.3.0`.

## Test Object

EXE:

`D:\HeiTang-Codex-WorkSpace\Project_01_HeiTang_KB_Forge\kb-forge-skill-ui\web\workbench\flutter_app\build\windows\x64\runner\Release\heitang_workbench.exe`

Input folder:

`D:\HeiTang-Codex-WorkSpace\input`

Runtime workspace:

`C:\Users\Administrator\AppData\Local\HeiTangKBForge\rc6_runtime_workspace`

## Product Flow Retest

| Step | Expected result |
| --- | --- |
| Launch EXE | Opens without white screen, black screen, or crash. |
| Dashboard | Shows product overview and recent work, not a debug command center. |
| Import & Parsing | Owns file/folder import, Owner input import, parse/OCR/chunking, and recovery. |
| Document Library | Shows imported files, parsing state, source, version, and references without overlap or mojibake. |
| Knowledge Base | Builds/rebuilds KB and shows real chunks, quality, local index, and artifact controls. |
| Retrieval & Verification | Runs real local retrieval and shows evidence/citation rows. |
| Document Generation | Generates or shows Markdown reading notes from the KB. |
| Skill Factory | Generates and shows Skill package structure and governance state. |
| Agent Factory | Creates Agents, binds KB/Skill, and runs multi-agent discussion only. No import/search/Markdown/Skill button wall should appear here. |
| Reports & Audit | Shows read-only evidence and reports, not the main workflow. |
| Settings | Shows workspace, Provider/storage, config, masked API key, local index, and optional external vector DB state. |

## Must Pass

| Item | Expected |
| --- | --- |
| Main capability yellow status | No yellow/pending for import, document library, KB, retrieval, document generation, Skill, Agent, or Settings save/storage. |
| Safety boundaries | Computer Use, arbitrary shell, unauthorized plugin marketplace, raw secret display remain disabled or absent. |
| Core command exposure | No `build-knowledge-base`, `generate-md`, `allowed_core_bridge`, or generic `运行 Core 操作` on main pages. |
| Tab behavior | Tabs visibly change content and selected state. |
| Button feedback | Main buttons show real state, generated artifact, or explicit disabled reason. |
| Real artifacts | `source_manifest.json`, `parse_report.json`, `chunks.jsonl`, `cards.jsonl`, `qa_pairs.jsonl`, `quality_report.json`, `reading_notes.md`, `SKILL.md`, `agent_manifest.json`, and `multi_agent_discussion.md` exist and are non-empty. |
| Persistence | Relaunch keeps imported files and generated product state visible. |

## Known Retest Notes

- Local validation used a 1280x720 desktop at 150% Windows scaling. Flutter widget tests cover 1366x768 and 1440x900 target layouts.
- The runtime workspace name remains `rc6_runtime_workspace`; rc7 is a UI capability-mapping repair over the accepted runtime chain.
- External vector DB remains optional and configurable. Local index is the accepted default path.

## Expected Owner Decision

If the above passes:

`v4.3.0-rc7_owner_retest_passed_pending_stable_release_decision`

If any main-flow capability is still misplaced, fake-enabled, yellow/pending, or not runnable:

`v4.3.0-rc7_owner_retest_failed_pending_rc8_scope`
