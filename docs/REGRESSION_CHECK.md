# Regression Check

v2.5 regression check covers v1.6 through v2.4 by checking local commands, modules, schemas, and test evidence.

It writes `regression_result.json`, `regression_report.md`, `regression_cases.jsonl`, and `regression_failures.jsonl`.

It does not rerun external runtimes or call online services.

