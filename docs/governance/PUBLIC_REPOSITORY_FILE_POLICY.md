# Public Repository File Policy

Tracked release-facing files must exclude local dependency directories, caches, current-run outputs, latest audit outputs, secrets, cookies, credentials, and large runtime binaries unless explicitly approved by a later gate.
