# Release Package

`release-package` creates a local release snapshot from a workspace.

The output includes `release_manifest.json` and selected stable workspace reports. It is a local artifact bundle and does not publish to any platform.
