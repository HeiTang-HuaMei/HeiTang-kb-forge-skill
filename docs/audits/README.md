# Audits 历史文档说明

历史文档，仅作追溯；当前产品基线以 `docs/product/` 下三份 v3 文档为准。本文目录下旧 rc、旧验收报告、旧能力矩阵和旧 UI 调整计划不得作为当前功能、架构或验收事实源。

当前主链路统一为：

```text
文档库 → 知识库 → 索引层 → RAG → 编排层 → 文档/Skill/Agent/A2A
```

如旧文件出现 v4.3.0-rc、release ready、runtime ready、OKF 等表述，均按历史语境理解，不能替代当前 v3 三基线。
