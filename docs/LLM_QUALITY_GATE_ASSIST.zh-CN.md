# LLM Quality Gate Assist

v2.5 新增 mock-first LLM quality gate assist。

输出 `llm_quality_gate_assist_result.json`、`llm_quality_gate_assist_report.md` 和 `llm_quality_gate_suggestions.jsonl`。

默认 provider 是 `mock`。它不需要 API key，不访问网络，不替代规则门禁，也不替代人工复核。

真实 LLM 治理和 live smoke 预留到 v2.6。

