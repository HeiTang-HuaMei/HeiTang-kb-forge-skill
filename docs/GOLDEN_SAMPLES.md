# Golden Samples

v2.5 adds local golden sample validation for knowledge packages, Skill packages, Agent packages, workspace snapshots, and platform export packages.

It writes `golden_sample_registry.json`, `golden_sample_validation.json`, `golden_sample_validation_report.md`, and `golden_sample_diff.json`.

Golden samples are local validation fixtures, not real platform integrations.

