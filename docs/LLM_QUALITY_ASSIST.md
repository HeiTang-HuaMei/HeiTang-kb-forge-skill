# LLM Quality Assist

LLM quality assist is optional and mock/fallback-safe by default.

It can generate review suggestions and eval-case generation reports, but it must not replace evidence, overwrite human review, or require a real API key.
