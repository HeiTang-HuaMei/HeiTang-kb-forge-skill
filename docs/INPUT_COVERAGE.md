# Input Coverage

v2.1 adds opt-in input coverage reporting with `source_inventory_enhanced.json`, `input_coverage_report.md`, and parser status summaries.

HTML, EPUB, and ZIP text ingestion are lightweight local parsers. ZIP ingestion extracts supported text-like files and preserves the internal source name in the parsed text.
