# Reliability Score

`reliability-score` generates `reliability_score.json` and `reliability_report.md` for a local workspace.

The score is rule-based and offline. It checks workspace completeness, package registration, Skill and Agent availability, provider registry presence, and audit trace availability. It is a release readiness signal, not an online evaluation system.
