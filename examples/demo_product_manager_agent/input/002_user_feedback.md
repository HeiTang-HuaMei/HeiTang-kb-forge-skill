# 用户反馈片段

用户 A：
我不是不会用大模型，而是不知道怎么把业务资料整理成 Agent 能稳定使用的知识库。

用户 B：
如果能把一本书、一个课程、一个商品资料包，自动生成 chunks、QA、术语和 RAG 输入文件，就能节省很多前期工作。

用户 C：
我希望看到质量报告，不然不知道生成结果能不能直接用于 Agent。

用户 D：
我更关心能不能一键生成适合产品经理、导购、教育伴学等不同场景的 Agent 模板。
