# Learning Objectives

Students should understand how local knowledge bases are built from source materials.

The pipeline reads documents, cleans text, creates chunks, validates records, and exports UTF-8 files.
