# AnySearchSkill UI Impact

The existing provider surface now shows:

- `provider_adapter`
- `real_smoke_passed`
- `needs_strengthening`
- network required
- API key optional
- `ui_configuration_pending`

No UI action is enabled yet. The UI must not claim local/offline readiness, proxy smoke success, complete API/proxy configuration, or release readiness.

The later UI, Core Bridge, and configuration campaigns must add settings, check, smoke, run, result, repair suggestion, and report export workflows.
