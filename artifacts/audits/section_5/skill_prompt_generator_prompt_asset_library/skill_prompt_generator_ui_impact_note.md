# skill-prompt-generator UI Impact Note

UI may display `real_integration` only with the boundary `prompt_asset_library_enhancer`.

Allowed surfaces:

- Skill Factory status card
- Template Library status card
- Prompt card preview from local artifacts
- License gate pending note

UI must not show the external runtime as ready, must not claim copied external prompts, and must not mark the full UI workflow or Campaign 3 complete.
